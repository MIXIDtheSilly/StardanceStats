STATS_API=http://127.0.0.1:8471/v1
PORT=3000

# Behind a proxy getClientAddress() returns the proxy, so every visitor would
# share one Ask rate-limit bucket. Set these to the hop count in front of us.
# ADDRESS_HEADER=x-forwarded-for
# XFF_DEPTH=1
